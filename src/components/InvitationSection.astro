---
export interface Props {
  id: string;
  additionalClasses?: string;
}

const { id, additionalClasses = '' } = Astro.props;
---

<section 
  class={`h-full p-8 flex flex-col justify-center snap-start relative ${additionalClasses}`} 
  id={id}
>
  <slot />
</section> 